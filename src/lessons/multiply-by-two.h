#pragma once

// header files are commonly used to hold forward declarations
// header files will violate the one-definition rule if a function of variable is DEFINED and then
// the header file is subsequently used in more than one source file

void multiply_by_two();
